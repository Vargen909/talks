"use client";

import { TalksBrandLoading } from "@/components/brand/talks-brand-loading";

/** Route-level loading — global brand loader (three dots + caption). */
export function RouteLoading() {
  return <TalksBrandLoading />;
}
